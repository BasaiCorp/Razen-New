// Perfect test for enhanced diagnostics

fun main() {
    // This should work fine
    println("Starting test")
    
    // Undefined variable error - should point to 'undefined_var'
    var result = undefined_var + 5
    
    // Type mismatch - should work (string concatenation)
    var text = "hello"
    var number = text + 42
    
    // Wrong function name - should point to 'print_line'
    print_line("This should suggest println")
    
    // Correct println usage
    println("This is correct")
    
    // Unused variable warning
    var unused_var = 10
    
    println("Test completed")
}
