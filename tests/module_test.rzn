// tests/module_test.rzn - Test module system imports (Go-style)

// Simple module import (Go-style approach)
use "utils"

// PLACEHOLDER: Selective imports (future implementation)
// use { helper_function, Config, MAX_CONNECTIONS } from "utils"

// PLACEHOLDER: Aliased import (future implementation)
// use "utils" as util_lib

fun main() {
    // Test imported function with namespace (Go-style)
    var result = utils.helper_function("test data")
    println(result)
    
    // Test imported struct with namespace
    var config = utils.Config.new("localhost", 8080)
    var url = config.get_url()
    println("Server URL: " + url)
    
    // Test imported constant with namespace
    println("Max connections: " + utils.MAX_CONNECTIONS)
    
    // Test that private function is not accessible
    // utils.private_function()  // This should cause an error
}
