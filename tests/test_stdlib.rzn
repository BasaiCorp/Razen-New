// Standard Library Test
// Testing all stdlib modules: string, math, array, file, json

fun main() {
    println("[INFO] Testing Razen Standard Library")
    println("=" * 50)
    
    // Test String Library
    println("\n[TEST] String Library")
    var text = "Hello World"
    println(f"Original: {text}")
    
    // Note: stdlib functions will be called as: string.upper(text)
    // This requires runtime integration which we'll add next
    
    // Test Math Library  
    println("\n[TEST] Math Library")
    var num = -5.7
    println(f"Number: {num}")
    
    // Test Array Library
    println("\n[TEST] Array Library")
    var arr = [1, 2, 3, 4, 5]
    println(f"Array: {arr}")
    
    // Test File Library
    println("\n[TEST] File Library")
    println("File operations ready")
    
    // Test JSON Library
    println("\n[TEST] JSON Library")
    var data = {"name": "Razen", "version": "0.1"}
    println(f"Data: {data}")
    
    println("\n[SUCCESS] Standard Library modules loaded successfully!")
    println("Note: Full stdlib integration requires runtime updates")
}
