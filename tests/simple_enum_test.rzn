// ===== SIMPLE ENUM TEST =====
// Testing enum variants

enum Color {
    Red,
    Green,
    Blue,
    RGB(int, int, int)
}

fun main() {
    println("=== SIMPLE ENUM TEST ===")
    
    // Test simple enum variants
    var color1 = Color.Red
    var color2 = Color.Green
    
    println(f"Color 1: {color1}")
    println(f"Color 2: {color2}")
    
    // Test enum with parameters (when implemented)
    // var color3 = Color.RGB(255, 128, 0)
    // println(f"Color 3: {color3}")
    
    println("=== ENUM TEST COMPLETED ===")
}
