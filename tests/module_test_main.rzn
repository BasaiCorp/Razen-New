// tests/module_test_main.rzn - Test main file that imports modules

use "./calculator"
// use "./utils_simple" as utils

fun main() {
    // Test calculator module
    var result = calculator.Add(5, 3)
    println(f"Calculator result: {result}")
    
    // Test utils module with alias
    // var text = utils.ToUpper("hello world")
    // println(f"Utils result: {text}")
    
    // Test built-in modules
    println("Built-in fmt module working!")
    
    println("Module system test completed successfully!")
}
