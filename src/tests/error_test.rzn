// Test file with intentional errors for diagnostic system testing

fun main() {
    // Missing semicolon
    var x: int = 42
    var y: str = "hello"
    
    // Undefined variable
    println(undefined_var)
    
    // Missing closing brace
    if x > 0 {
        println("positive")
    // Missing closing brace here
    
    // Invalid syntax
    var z: = 
    
    // Missing function parameter type
    fun broken_func(param) {
        return param
    }
    
    // Unterminated string
    var bad_string = "unterminated
    
    println("This should still parse")
}
