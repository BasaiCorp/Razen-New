fun main() {
    var name = "World"
    var greeting = f"Hello, {name}!"
    println(greeting)
    
    var age = 25
    var message = f"I am {age} years old"
    println(message)
    
    // Test with direct f-string
    println(f"Direct f-string with {name}")
}
