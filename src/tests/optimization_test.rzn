fun add(a: int, b: int) -> int {
    return a + b
}

fun main() {
    var x: int = 5 + 3
    var y: int = x * 1
    var z: int = y + 0
    var unused: int = 42
    
    println("Optimization test complete")
}
